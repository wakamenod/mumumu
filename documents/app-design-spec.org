#+TITLE: クイズ形式暗算・学習アプリ 開発設計書
#+AUTHOR: アプリ開発プロジェクト
#+DATE: 2026-06-14
#+OPTIONS: toc:nil

* 1. 概要
本アプリは、React Native と Firebase を組み合わせたクイズ形式の学習・暗算アプリである。
小学校1年生レベルから大学レベルまでの難易度を選択し、全20問のクイズに回答する。
スコア（正解数・経過時間）に応じて、アーケードゲームのように上位20名がランキングに名前を登録できる仕組みを持つ。
最小の運用コスト（ほぼ無料枠内）での運用と、チート（不正行為）の完全な防止を両立する設計とする。

* 2. アーキテクチャと量産戦略
** 2.1 テクノロジー構成
- フロントエンド: React Native (TypeScript)
- バックエンド/サーバー: Firebase (Cloud Functions, Firebase Storage, Firestore)

** 2.2 量産化戦略 (Flavor / Target の採用)
ソースコード（プログラム）は完全に1つに共通化し、Androidの「Flavor」およびiOSの「Target」機能を採用する。
これにより、以下の専門ジャンルアプリを設定の切り替えだけで一括ビルド・量産可能とする。
- 「暗算クイズアプリ」
- 「歴史年号クイズアプリ」
- 「英単語クイズアプリ」
- 「漢字・元素記号クイズアプリ」

* 3. データ管理と配信設計 (パターン2の採用)
** 3.1 問題データの配置
問題データは複数箇所で管理せず、サーバー側（Firebase Storage）にジャンル・難易度ごとのJSONファイルとして一元管理する。
アプリ側には問題データを直接同梱せず、起動時またはクイズ開始時に取得する。

** 3.2 JSONデータの構造（チート対策：ハッシュ化）
スマホメモリからの答えの盗み見を防ぐため、JSONには生の正解を持たせず、不可逆な「ハッシュ値（暗号）」を格納する。
#+BEGIN_SRC json
{
  "question": "5 + 7 = ?",
  "answer_hash": "ed076287532e82e1d668..."
}
#+END_SRC

** 3.3 サーバー側でのキャッシュ管理（コスト削減）
Cloud Functions のインスタンス永続性を利用し、一度 Storage からダウンロードしたJSONファイルを Functions 側のメモリにキャッシュ（一時保存）する。
これにより、ユーザー数が増加しても Storage の通信量をほぼゼロ（無料枠内）に抑え込む。
- *問題更新時のキャッシュクリア手法*: JSONファイルのURLにバージョン（例: =_v2.json=）を含め、Functions のデプロイ時に数値を切り替えることで古いキャッシュを強制破棄する。
- もしくはキャッシュに「有効期限（TTL）」を設定する

* 4. 画面仕様とゲームループ
** 4.1 画面フロー
1. *難易度選択画面*: 小1〜大学レベルを選択。スタートボタンでクイズ開始。
2. *クイズ画面*: 1問ごとにユーザーが回答。
3. *結果表示・ランキング画面*: 通信結果を受け取り、上位20位以内なら名前入力フォームを表示。

ルーティング構成（Expo Router）:
#+BEGIN_EXAMPLE
Stack (app/_layout.tsx)
├── (tabs)/index  →  /        難易度選択画面（初期画面）
├── quiz          →  /quiz    クイズ画面（levelId パラメータで難易度を受け取る）
└── modal         →  /modal
#+END_EXAMPLE

** 4.2 難易度選択画面の UI 仕様
*ステッパー形式の大カード* を採用する。

- 中央に選択中のレベルを大きく表示し、左右の矢印ボタンでレベルを切り替える
- レベル切り替え時はスライドアニメーション（react-native-reanimated v4）で演出する
- 画面下部のドットインジケーターで全9段階中の現在位置を示す
- スタートボタンは常に同一画面に表示し、操作ステップを最小化する

** 4.3 難易度レベル定義
全9段階。ID は Firebase Firestore のコレクションキーおよびクイズ開始時の =levelId= パラメータと対応する。

| ID         | 表示名   | 難易度（星） | 出題内容           |
|------------+----------+--------------+--------------------|
| grade1     | 小学1年生 | ★           | 1桁のたし算・ひき算 |
| grade2     | 小学2年生 | ★           | 2桁の計算・九九     |
| grade3     | 小学3年生 | ★★          | かけ算・わり算の基礎 |
| grade4     | 小学4年生 | ★★          | 小数・分数の基礎    |
| grade5     | 小学5年生 | ★★★         | 分数の計算・面積    |
| grade6     | 小学6年生 | ★★★         | 比・速さ・比例      |
| middle     | 中学生    | ★★★★        | 正負・方程式・平方根 |
| high       | 高校生    | ★★★★        | 二次方程式・三角関数 |
| university | 大学生    | ★★★★★       | 微分・積分・行列    |

** 4.4 1問ごとの正誤判定（UXの最適化）
テンポの良さを重視し、1問ごとにサーバーを叩く通信は行わない。
アプリ側で「ユーザーの入力値」をハッシュ化し、JSON内の =answer_hash= と一致するかをローカルで比較して、その場でマルバツの演出を出す。

* 5. 鉄壁のセキュリティ・チート対策
** 5.1 想定されるチート手口への防御
- *手口1：関数のフッキング（True/False の書き換え）*
  - *影響*: アプリ画面上はすべて「正解」の演出に書き換えられる。
  - *防御*: 最終スコアは自己申告データではなく、ユーザーが入力した「生の解答リスト」をサーバーに送り、Functions 側で厳密に一から答え合わせを行うため、ランキング登録は100%阻止される。
- *手口2：メモリのスキャン（答えの自動割り出し・マクロ自動入力）*
  - *防御*: スマホメモリ内には暗号（ハッシュ値）しか存在しないため、スクリプトによる答えの自動検出は不可能。

** 5.2 通信データの改ざん・タイマー不正の防御
クイズ終了時に「解答リスト」と「経過時間」を Cloud Functions に送信。
Functions 側で「クイズ開始時のタイムスタンプ」と「終了時のタイムスタンプ」の差分をサーバー側で引き算し、リアルな経過時間を算出。
人間には不可能な異常タイム（例: 0.1秒など）や、解答リストの不正はサーバー側で破棄する。

* 6. データベース設計 (Firebase Firestore)
ランキング情報は、ソート速度と管理効率を考慮し、難易度ごとにサブコレクションを分ける構造とする。

** 6.1 コレクション構造
#+BEGIN_EXAMPLE
/rankings (コレクション)
    └ /level_1 (ドキュメント: 難易度)
        └ /scores (サブコレクション)
            └ /doc_id (自動生成ID)
                ├ username: "AAA" (文字列)
                ├ correct_count: 20 (数値: 正解数)
                ├ elapsed_time: 45.2 (数値: 経過時間・秒)
                └ created_at: 2026-06-13T09:27:00Z (タイムスタンプ)
#+END_EXAMPLE

** 6.2 ランキング判定クエリ
Firestoreの複合インデックスを作成し、以下のソート条件で上位20件を抽出する。
1. =correct_count= を *降順* （正解数が多い順）
2. =elapsed_time= を *昇順* （タイムが短い順）
取得した20位のスコアよりも今回のスコアが勝っている場合、ランクインと判定する。

* 7. 運用・コスト管理
** 7.1 コスト見込み
通常運用（月間数万プレイ規模）であれば、Cloud Functions・Firestore・Storage の無料枠内にほぼ収まるため、固定費（Artifact Registry の保管料数十円/月）のみで運用可能。
** 7.2 安全対策
開発中およびリリース後の事故を防ぐため、Firebaseに「予算アラート（例: 500円超過で通知）」を必ず設定し、バグによる無限ループ等の課金スパイクを防止する。
